---
phase: 30-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/infrastructure/database/drizzle/schema/ingestion-batches.schema.ts
  - apps/api/src/core/entities/batch.entity.ts
  - apps/api/src/infrastructure/database/drizzle/mappers/batch.mapper.ts
autonomous: true

must_haves:
  truths:
    - "Batch schema has identifierFieldKey column (nullable varchar)"
    - "Batch schema has secondaryFieldKey column (nullable varchar)"
    - "Batch entity includes identifier fields"
    - "Batch mapper handles identifier field mapping"
  artifacts:
    - path: "apps/api/src/infrastructure/database/drizzle/schema/ingestion-batches.schema.ts"
      provides: "Batch schema with identifier columns"
      contains: "identifierFieldKey"
    - path: "apps/api/src/core/entities/batch.entity.ts"
      provides: "Batch entity with identifier fields"
      contains: "identifierFieldKey"
    - path: "apps/api/src/infrastructure/database/drizzle/mappers/batch.mapper.ts"
      provides: "Mapper for identifier fields"
      contains: "identifierFieldKey"
  key_links:
    - from: "apps/api/src/infrastructure/database/drizzle/mappers/batch.mapper.ts"
      to: "apps/api/src/infrastructure/database/drizzle/schema/ingestion-batches.schema.ts"
      via: "imports and maps schema columns"
      pattern: "identifierFieldKey.*identifierFieldKey"
---

<objective>
Add identifier field columns to batch schema and update domain entity to support meaningful row identification in the extension.

Purpose: Batches need to store which columns serve as primary and secondary identifiers so the extension can show meaningful labels (e.g., "CNPJ: 12.345.678/0001-90") instead of just "Row 1".

Output: Database schema with identifier columns, domain entity with identifier fields, mapper that handles the new fields.
</objective>

<execution_context>
@/Users/luanmartins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luanmartins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-backend-foundation/30-CONTEXT.md

# Existing files to modify
@apps/api/src/infrastructure/database/drizzle/schema/ingestion-batches.schema.ts
@apps/api/src/core/entities/batch.entity.ts
@apps/api/src/infrastructure/database/drizzle/mappers/batch.mapper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add identifier columns to batch schema and generate migration</name>
  <files>apps/api/src/infrastructure/database/drizzle/schema/ingestion-batches.schema.ts</files>
  <action>
Add two nullable varchar columns to the ingestionBatches table:
- `identifierFieldKey`: varchar(255), nullable - stores the column key for primary identifier
- `secondaryFieldKey`: varchar(255), nullable - stores the column key for secondary identifier

These columns store the normalizedKey from columnMetadata (e.g., "cpf", "cnpj", "nome") that will be used to look up values in row data.

After modifying the schema, generate the Drizzle migration:
```bash
cd apps/api && npx drizzle-kit generate
```

Update the IngestionBatchRow and IngestionBatchInsert types (they're auto-inferred from the schema).
  </action>
  <verify>
Run `cd apps/api && npx drizzle-kit generate` - should create new migration file.
Verify migration SQL includes `ALTER TABLE ... ADD COLUMN identifier_field_key` and `secondary_field_key`.
  </verify>
  <done>
Migration file exists in apps/api/drizzle/ with identifier column additions.
Schema file has identifierFieldKey and secondaryFieldKey columns defined.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update batch entity and mapper with identifier fields</name>
  <files>
apps/api/src/core/entities/batch.entity.ts
apps/api/src/infrastructure/database/drizzle/mappers/batch.mapper.ts
  </files>
  <action>
**In batch.entity.ts:**
Add to the Batch interface:
- `identifierFieldKey: string | null;`
- `secondaryFieldKey: string | null;`

Add to CreateBatchData interface (both optional):
- `identifierFieldKey?: string | null;`
- `secondaryFieldKey?: string | null;`

Create a new UpdateBatchData interface:
```typescript
export interface UpdateBatchData {
  name?: string;
  identifierFieldKey?: string | null;
  secondaryFieldKey?: string | null;
}
```

**In batch.mapper.ts:**
Update the toDomain method to map the new fields:
- `identifierFieldKey: row.identifierFieldKey ?? null`
- `secondaryFieldKey: row.secondaryFieldKey ?? null`

The mapper should handle both the schema row type and the domain entity type.
  </action>
  <verify>
TypeScript compiles without errors: `cd apps/api && npx tsc --noEmit`
  </verify>
  <done>
Batch interface has identifierFieldKey and secondaryFieldKey fields.
UpdateBatchData interface exists with optional name and identifier fields.
Mapper correctly maps identifier fields from schema to domain.
  </done>
</task>

</tasks>

<verification>
1. Schema file has both identifier columns defined
2. Migration file exists with correct ALTER TABLE statements
3. Batch entity has identifier fields
4. Mapper handles identifier field mapping
5. TypeScript compiles: `cd apps/api && npx tsc --noEmit`
</verification>

<success_criteria>
- [ ] identifierFieldKey column added to schema (nullable varchar)
- [ ] secondaryFieldKey column added to schema (nullable varchar)
- [ ] Migration file generated
- [ ] Batch entity updated with identifier fields
- [ ] UpdateBatchData interface created
- [ ] Mapper updated to handle identifier fields
- [ ] TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-backend-foundation/30-01-SUMMARY.md`
</output>
