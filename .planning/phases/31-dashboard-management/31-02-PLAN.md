---
phase: 31-dashboard-management
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - apps/web/app/(platform)/projects/[id]/page.tsx
  - apps/web/components/projects/batch-detail-header.tsx
  - apps/web/components/projects/batch-card.tsx
  - apps/web/components/projects/batch-settings-modal.tsx
  - apps/web/components/projects/delete-batch-dialog.tsx
autonomous: true

must_haves:
  truths:
    - "User can edit project name inline on project detail page"
    - "User can edit batch name inline on batch detail header"
    - "Gear icon on batch card opens batch settings modal"
    - "Batch settings modal shows identifier dropdowns with live preview"
    - "Delete button on batch card opens confirmation dialog"
    - "Confirmed delete soft-removes batch and refreshes list"
  artifacts:
    - path: "apps/web/components/projects/batch-settings-modal.tsx"
      provides: "Batch settings modal with identifier configuration"
      exports: ["BatchSettingsModal"]
    - path: "apps/web/components/projects/delete-batch-dialog.tsx"
      provides: "Delete batch confirmation dialog"
      exports: ["DeleteBatchDialog"]
  key_links:
    - from: "apps/web/components/projects/batch-card.tsx"
      to: "BatchSettingsModal"
      via: "settings icon click opens modal"
      pattern: "setSettingsModalOpen"
    - from: "apps/web/components/projects/batch-card.tsx"
      to: "useDeleteBatch"
      via: "delete confirmation triggers mutation"
      pattern: "deleteBatch.mutate"
---

<objective>
Wire inline editing and batch CRUD UI

Purpose: Connect InlineEditName to project/batch names, add batch settings modal with identifier config, and add batch delete flow
Output: Working inline edit for names, batch settings modal, batch delete with confirmation
</objective>

<execution_context>
@/Users/luanmartins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luanmartins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-dashboard-management/31-CONTEXT.md

# Reference from Plan 01
@apps/web/components/projects/inline-edit-name.tsx
@apps/web/lib/query/hooks/use-batches.ts
@apps/web/lib/api/schemas/batch.schema.ts

# Existing patterns
@apps/web/components/projects/delete-project-dialog.tsx
@apps/web/components/projects/batch-detail-header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline edit to project detail page</name>
  <files>apps/web/app/(platform)/projects/[id]/page.tsx</files>
  <action>
Replace the static project.name display in AppHeader with InlineEditName:

1. Import InlineEditName from @/components/projects/inline-edit-name
2. Import useUpdateProject hook from @/lib/query/hooks/use-projects
3. Initialize the mutation: const updateProject = useUpdateProject()

4. Replace AppHeader title prop with:
   <InlineEditName
     value={project.name}
     onSave={async (name) => {
       await updateProject.mutateAsync({ id: project.id, data: { name } });
     }}
     isLoading={updateProject.isPending}
   />

5. Keep the existing children (Upload button) inside AppHeader

The AppHeader component should already accept ReactNode for title or you may need to adjust. If AppHeader only accepts string, wrap InlineEditName in a div and pass as children, adjusting layout as needed.
  </action>
  <verify>
Check InlineEditName is used in project detail:
```bash
grep "InlineEditName" apps/web/app/\(platform\)/projects/\[id\]/page.tsx
```
  </verify>
  <done>Project name is editable inline using InlineEditName component</done>
</task>

<task type="auto">
  <name>Task 2: Create batch settings modal and delete dialog</name>
  <files>apps/web/components/projects/batch-settings-modal.tsx, apps/web/components/projects/delete-batch-dialog.tsx</files>
  <action>
Create BatchSettingsModal (batch-settings-modal.tsx):

Props:
- open: boolean
- onOpenChange: (open: boolean) => void
- batch: BatchResponse | null
- projectId: string
- onSave: () => void (callback after successful save)

Content:
1. Use Dialog from shadcn/ui
2. DialogHeader with title "Configuracoes da Importacao"
3. Two Select components (shadcn/ui) for identifier configuration:
   - Primary identifier: dropdown with batch.columnMetadata options (show originalHeader, value=normalizedKey)
   - Secondary identifier: same options
   - Include "Nenhum" option with value="" for clearing
4. Live preview section:
   - Fetch first row via useBatchRows(projectId, batch.id, 1, 0)
   - Show formatted preview: "Ex: {primaryValue} - {secondaryValue}" or just primary if no secondary
   - Handle loading state with Skeleton
5. Save button calls useUpdateBatch mutation with identifierFieldKey and secondaryFieldKey
6. Close modal on successful save, call onSave callback
7. Show loading spinner while saving

Create DeleteBatchDialog (delete-batch-dialog.tsx):

Follow exact pattern from delete-project-dialog.tsx:
- Props: open, onOpenChange, onConfirm, batch (BatchResponse | null), isPending
- Warning icon, title "Excluir importacao", description about permanent action
- Confirm button triggers onConfirm callback
  </action>
  <verify>
Check both components exist:
```bash
ls -la apps/web/components/projects/batch-settings-modal.tsx apps/web/components/projects/delete-batch-dialog.tsx
```
  </verify>
  <done>BatchSettingsModal and DeleteBatchDialog components exist with proper implementations</done>
</task>

<task type="auto">
  <name>Task 3: Wire batch card with settings and delete actions</name>
  <files>apps/web/components/projects/batch-card.tsx</files>
  <action>
Add settings and delete functionality to BatchCard:

1. Add new props:
   - onSettingsClick?: (batch: BatchResponse) => void
   - onDeleteClick?: (batch: BatchResponse) => void

2. Add action buttons row at bottom of card content (before ChevronRight):
   - Settings button: Settings icon (lucide-react), calls onSettingsClick
   - Delete button: Trash2 icon (lucide-react), calls onDeleteClick
   - Style: ghost variant, opacity-0 group-hover:opacity-100 (same as ProjectCard pattern)
   - Wrap in onClick stopPropagation div to prevent Link navigation

3. Keep the existing Link wrapper for card navigation

4. Button styling: size="icon-xs" or similar small icon button
  </action>
  <verify>
Check new icons are imported and used:
```bash
grep -E "Settings|Trash2" apps/web/components/projects/batch-card.tsx
```
  </verify>
  <done>BatchCard shows settings and delete buttons on hover</done>
</task>

</tasks>

<verification>
- Project detail page uses InlineEditName for project name
- BatchSettingsModal opens with identifier dropdowns and live preview
- DeleteBatchDialog follows existing delete dialog pattern
- BatchCard shows settings/delete icons on hover
- TypeScript compilation passes
</verification>

<success_criteria>
- Clicking pencil on project name enters edit mode
- Saving project name calls API and updates display
- Gear icon on batch card opens settings modal
- Settings modal shows column options and preview
- Trash icon opens delete confirmation
- Confirming delete removes batch and refreshes list
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-management/31-02-SUMMARY.md`
</output>
