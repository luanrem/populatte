---
phase: 31-dashboard-management
plan: 03
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - apps/web/components/projects/batch-grid.tsx
  - apps/web/components/projects/batch-detail-header.tsx
  - apps/web/app/(platform)/projects/[id]/batches/[batchId]/page.tsx
autonomous: true

must_haves:
  truths:
    - "BatchGrid manages settings modal and delete dialog state"
    - "Batch detail header shows inline-editable name"
    - "Settings and delete work end-to-end on batch grid"
  artifacts:
    - path: "apps/web/components/projects/batch-grid.tsx"
      provides: "Batch grid with settings modal and delete dialog integration"
      contains: "BatchSettingsModal|DeleteBatchDialog"
    - path: "apps/web/components/projects/batch-detail-header.tsx"
      provides: "Batch header with inline edit for name"
      contains: "InlineEditName"
  key_links:
    - from: "apps/web/components/projects/batch-grid.tsx"
      to: "useDeleteBatch"
      via: "delete confirmation handler"
      pattern: "deleteBatch.mutate"
    - from: "apps/web/components/projects/batch-detail-header.tsx"
      to: "useUpdateBatch"
      via: "inline edit save handler"
      pattern: "updateBatch.mutateAsync"
---

<objective>
Integrate batch CRUD into grid and detail views

Purpose: Wire BatchSettingsModal and DeleteBatchDialog into BatchGrid, add inline edit to batch detail header
Output: Complete batch management UI with settings, delete, and name editing
</objective>

<execution_context>
@/Users/luanmartins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luanmartins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-dashboard-management/31-CONTEXT.md

# Reference from prior plans
@apps/web/components/projects/inline-edit-name.tsx
@apps/web/components/projects/batch-settings-modal.tsx
@apps/web/components/projects/delete-batch-dialog.tsx
@apps/web/components/projects/batch-card.tsx
@apps/web/lib/query/hooks/use-batches.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate modals into BatchGrid</name>
  <files>apps/web/components/projects/batch-grid.tsx</files>
  <action>
Update BatchGrid to manage modal state and wire to BatchCards:

1. Add state:
   - const [settingsOpen, setSettingsOpen] = useState(false)
   - const [deleteOpen, setDeleteOpen] = useState(false)
   - const [selectedBatch, setSelectedBatch] = useState<BatchResponse | null>(null)

2. Import and use useDeleteBatch hook:
   - const deleteBatch = useDeleteBatch(projectId)

3. Create handlers:
   - handleSettingsClick(batch: BatchResponse) - set selectedBatch, open settings modal
   - handleDeleteClick(batch: BatchResponse) - set selectedBatch, open delete dialog
   - handleDeleteConfirm() - call deleteBatch.mutate({ batchId: selectedBatch.id }), close dialog on success

4. Pass handlers to BatchCard:
   <BatchCard
     batch={batch}
     projectId={projectId}
     onSettingsClick={handleSettingsClick}
     onDeleteClick={handleDeleteClick}
   />

5. Render modals at end of component:
   <BatchSettingsModal
     open={settingsOpen}
     onOpenChange={setSettingsOpen}
     batch={selectedBatch}
     projectId={projectId}
     onSave={() => setSettingsOpen(false)}
   />
   <DeleteBatchDialog
     open={deleteOpen}
     onOpenChange={setDeleteOpen}
     batch={selectedBatch}
     onConfirm={handleDeleteConfirm}
     isPending={deleteBatch.isPending}
   />

6. Add success toast on delete: toast.success("Importacao excluida")
  </action>
  <verify>
Check modals are imported and rendered:
```bash
grep -E "BatchSettingsModal|DeleteBatchDialog" apps/web/components/projects/batch-grid.tsx
```
  </verify>
  <done>BatchGrid manages settings and delete modals with proper state handling</done>
</task>

<task type="auto">
  <name>Task 2: Add inline edit to batch detail header</name>
  <files>apps/web/components/projects/batch-detail-header.tsx</files>
  <action>
Update BatchDetailHeader to use InlineEditName for the batch name:

1. Add new props:
   - projectId: string (already exists)
   - onNameChange?: (newName: string) => Promise<void>
   - isUpdating?: boolean

2. Replace the static batch name display:
   - Current: <h2 className="text-xl font-semibold">{batch.name ?? "Sem nome"}</h2>
   - Replace with:
   <InlineEditName
     value={batch.name ?? "Sem nome"}
     onSave={onNameChange ?? (async () => {})}
     isLoading={isUpdating}
     className="text-xl font-semibold"
   />

3. Keep the FileSpreadsheet icon before the name

Note: The parent page (batch detail page) will provide onNameChange callback.
  </action>
  <verify>
Check InlineEditName is used:
```bash
grep "InlineEditName" apps/web/components/projects/batch-detail-header.tsx
```
  </verify>
  <done>Batch detail header shows inline-editable name</done>
</task>

<task type="auto">
  <name>Task 3: Wire batch detail page with update mutation</name>
  <files>apps/web/app/(platform)/projects/[id]/batches/[batchId]/page.tsx</files>
  <action>
Update batch detail page to provide name update callback:

1. Import useUpdateBatch from @/lib/query/hooks/use-batches

2. Initialize mutation:
   const updateBatch = useUpdateBatch(id);

3. Create handler:
   const handleBatchNameChange = async (name: string) => {
     await updateBatch.mutateAsync({ batchId, data: { name } });
   };

4. Pass to BatchDetailHeader:
   <BatchDetailHeader
     batch={batch}
     projectId={id}
     projectName={project.name}
     onNameChange={handleBatchNameChange}
     isUpdating={updateBatch.isPending}
   />
  </action>
  <verify>
Check useUpdateBatch is imported and used:
```bash
grep "useUpdateBatch" apps/web/app/\(platform\)/projects/\[id\]/batches/\[batchId\]/page.tsx
```
  </verify>
  <done>Batch detail page wires name update to API via mutation</done>
</task>

</tasks>

<verification>
- BatchGrid renders settings modal and delete dialog
- Clicking settings on batch card opens modal with correct batch data
- Clicking delete and confirming removes batch and shows toast
- Batch detail header has inline-editable name
- Saving batch name calls API and updates display
- TypeScript compilation passes
</verification>

<success_criteria>
- Settings gear on batch card opens BatchSettingsModal
- Identifier dropdowns show column options from batch.columnMetadata
- Live preview updates when selecting identifiers
- Delete confirmation removes batch and refreshes grid
- Batch name inline edit works on detail page
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-management/31-03-SUMMARY.md`
</output>
