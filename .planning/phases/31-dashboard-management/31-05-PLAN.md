---
phase: 31-dashboard-management
plan: 05
type: execute
wave: 3
depends_on: ["31-02", "31-04"]
files_modified:
  - apps/web/components/projects/mappings-list.tsx
  - apps/web/components/projects/mappings-empty-state.tsx
  - apps/web/components/projects/delete-mapping-dialog.tsx
  - apps/web/components/projects/new-mapping-modal.tsx
autonomous: true

must_haves:
  truths:
    - "Mappings list shows name, URL, step count, and active status"
    - "Delete button opens confirmation dialog"
    - "Edit button navigates to mapping edit page"
    - "New mapping button opens instruction modal"
    - "Empty state shows when no mappings exist"
  artifacts:
    - path: "apps/web/components/projects/mappings-list.tsx"
      provides: "Mappings list component with table display"
      exports: ["MappingsList"]
    - path: "apps/web/components/projects/delete-mapping-dialog.tsx"
      provides: "Delete mapping confirmation dialog"
      exports: ["DeleteMappingDialog"]
    - path: "apps/web/components/projects/new-mapping-modal.tsx"
      provides: "Instruction modal for creating new mappings"
      exports: ["NewMappingModal"]
  key_links:
    - from: "apps/web/components/projects/mappings-list.tsx"
      to: "useMappings"
      via: "query hook for data"
      pattern: "useMappings"
    - from: "apps/web/components/projects/mappings-list.tsx"
      to: "useDeleteMapping"
      via: "mutation for delete action"
      pattern: "useDeleteMapping"
---

<objective>
Build mappings list UI components

Purpose: Display all mappings for a project with name, URL, step count, status, and CRUD actions
Output: MappingsList, DeleteMappingDialog, NewMappingModal, and empty state components
</objective>

<execution_context>
@/Users/luanmartins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luanmartins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-dashboard-management/31-CONTEXT.md

# Reference from Plan 04
@apps/web/lib/query/hooks/use-mappings.ts
@apps/web/lib/api/schemas/mapping.schema.ts

# Existing UI patterns
@apps/web/components/projects/delete-batch-dialog.tsx
@apps/web/components/ui/table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mappings list component</name>
  <files>apps/web/components/projects/mappings-list.tsx</files>
  <action>
Create MappingsList component to display mappings in a table:

Props:
- projectId: string

Implementation:
1. Use useMappings(projectId) hook to fetch data
2. Manage state for delete dialog and new mapping modal:
   - selectedMapping: MappingListItem | null
   - deleteDialogOpen: boolean
   - newMappingModalOpen: boolean
3. Use useDeleteMapping hook for delete action

Table display (using shadcn/ui Table components):
- Column 1: Name (text-sm font-medium)
- Column 2: URL (text-sm text-muted-foreground, truncate with max-w-xs)
- Column 3: Steps (Badge showing stepCount number)
- Column 4: Status (Badge - active=green "Ativo", inactive=gray "Inativo")
- Column 5: Actions (Edit and Delete icon buttons)

Action buttons:
- Edit: Pencil icon, navigates to /projects/{projectId}/mappings/{mappingId} (use Link from next/link)
- Delete: Trash2 icon, opens delete dialog with selected mapping

Header with "+ Novo Mapping" button that opens NewMappingModal

Handle loading state with Skeleton rows
Handle empty state by rendering MappingsEmptyState

On successful delete: show toast.success("Mapping excluido")
  </action>
  <verify>
Check component exports MappingsList:
```bash
grep "export.*MappingsList" apps/web/components/projects/mappings-list.tsx
```
  </verify>
  <done>MappingsList component displays table with actions</done>
</task>

<task type="auto">
  <name>Task 2: Create empty state and delete dialog</name>
  <files>apps/web/components/projects/mappings-empty-state.tsx, apps/web/components/projects/delete-mapping-dialog.tsx</files>
  <action>
Create MappingsEmptyState (mappings-empty-state.tsx):
- Simple centered layout with icon (Map or Layers from lucide-react)
- Title: "Nenhum mapping criado"
- Description: "Mappings sao criados pela extensao do navegador. Abra o site de destino e use a extensao para capturar os campos."
- Optional: "+ Novo Mapping" button that calls onNewClick prop

Create DeleteMappingDialog (delete-mapping-dialog.tsx):
Follow exact pattern from delete-batch-dialog.tsx:

Props:
- open: boolean
- onOpenChange: (open: boolean) => void
- onConfirm: () => void
- mapping: MappingListItem | null
- isPending?: boolean

Content:
- Warning icon (TriangleAlert)
- Title: "Excluir mapping"
- Description: "Esta acao nao pode ser desfeita."
- Body text: "Tem certeza que deseja excluir o mapping {mapping?.name}? Todos os passos associados serao removidos."
- Cancel and Delete buttons (Delete is destructive variant)
  </action>
  <verify>
Check both components exist:
```bash
ls -la apps/web/components/projects/mappings-empty-state.tsx apps/web/components/projects/delete-mapping-dialog.tsx
```
  </verify>
  <done>Empty state and delete dialog components created</done>
</task>

<task type="auto">
  <name>Task 3: Create new mapping instruction modal</name>
  <files>apps/web/components/projects/new-mapping-modal.tsx</files>
  <action>
Create NewMappingModal for instruction on creating mappings:

Props:
- open: boolean
- onOpenChange: (open: boolean) => void

Content (Dialog from shadcn/ui):
1. DialogHeader:
   - Title: "Criar novo mapping"
   - Description: "Mappings sao criados diretamente na extensao do navegador"

2. Body - numbered steps list:
   1. "Instale a extensao Populatte no Chrome"
   2. "Abra o site onde deseja preencher formularios"
   3. "Clique no icone da extensao e selecione este projeto"
   4. "Use o modo de captura para mapear os campos do formulario"

3. Optional helpful tips:
   - "O mapping sera associado automaticamente a URL do site"
   - "Voce pode editar os campos capturados aqui no dashboard"

4. DialogFooter:
   - Close button: "Entendi"

Keep it simple and informative. No complex interactions needed.
  </action>
  <verify>
Check component exists:
```bash
grep "export.*NewMappingModal" apps/web/components/projects/new-mapping-modal.tsx
```
  </verify>
  <done>NewMappingModal shows instructions for creating mappings via extension</done>
</task>

</tasks>

<verification>
- MappingsList renders table with correct columns
- Empty state appears when no mappings
- Delete dialog follows existing pattern
- New mapping modal explains extension workflow
- TypeScript compilation passes
</verification>

<success_criteria>
- Mappings table shows name, URL, step count, status
- Edit navigates to mapping edit page (route may 404 until Phase 32)
- Delete opens confirmation and removes mapping on confirm
- New mapping button shows helpful instructions
- Empty state guides users to extension
</success_criteria>

<output>
After completion, create `.planning/phases/31-dashboard-management/31-05-SUMMARY.md`
</output>
