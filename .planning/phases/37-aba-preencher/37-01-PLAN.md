---
phase: 37-aba-preencher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/extension/entrypoints/sidepanel/App.tsx
  - apps/extension/entrypoints/sidepanel/components/ConnectedIndicator.tsx
  - apps/extension/entrypoints/sidepanel/components/FillControls.tsx
  - apps/extension/entrypoints/sidepanel/components/preencher/PreencherStepList.tsx
  - apps/extension/entrypoints/sidepanel/components/preencher/index.ts
  - apps/extension/entrypoints/sidepanel/components/index.ts
  - apps/extension/src/api/mappings.ts
  - apps/extension/src/types/messages.ts
autonomous: true

must_haves:
  truths:
    - "Steps list appears below mapping name when a mapping is selected, showing count header, action icons, selector text, and source field per step"
    - "Steps list is collapsed by default and expandable via chevron toggle"
    - "Connection status shows as compact colored dot badge in header area (green=connected, red=disconnected)"
    - "Fill button is sticky at bottom of panel, always visible"
    - "Empty state shows guided prompt when no mapping selected"
    - "Summary indicator shows count of steps with invalid selectors"
    - "Steps are reorderable via drag-and-drop"
    - "Invalid selector steps show red dot badge with hover tooltip"
  artifacts:
    - path: "apps/extension/entrypoints/sidepanel/components/preencher/PreencherStepList.tsx"
      provides: "Collapsible, sortable steps list with action icons, selector text, source field, red dot badges, drag-and-drop"
    - path: "apps/extension/entrypoints/sidepanel/components/preencher/index.ts"
      provides: "Barrel export for Preencher components"
  key_links:
    - from: "apps/extension/entrypoints/sidepanel/App.tsx"
      to: "apps/extension/src/api/mappings.ts"
      via: "fetchMappingWithSteps call when mapping is selected"
      pattern: "fetchMappingWithSteps"
    - from: "apps/extension/entrypoints/sidepanel/App.tsx"
      to: "apps/extension/entrypoints/sidepanel/components/preencher/PreencherStepList.tsx"
      via: "Renders PreencherStepList with mapping steps data"
      pattern: "PreencherStepList"
---

<objective>
Restructure the Preencher tab layout for Side Panel width optimization and add a collapsible, sortable steps list that displays mapping step details with invalid selector indicators.

Purpose: Deliver FILL-01 (migrated fill workflow), FILL-02 (steps list), FILL-04 (warning badges), and FILL-05 (320px layout) requirements. This is the foundation for element highlighting (Plan 02).
Output: Restructured Preencher tab with compact layout, steps list component, and mapping steps fetch integration.
</objective>

<execution_context>
@/Users/luanmartins/.claude/get-shit-done/workflows/execute-plan.md
@/Users/luanmartins/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/extension/entrypoints/sidepanel/App.tsx
@apps/extension/entrypoints/sidepanel/components/ConnectedIndicator.tsx
@apps/extension/entrypoints/sidepanel/components/FillControls.tsx
@apps/extension/entrypoints/sidepanel/components/MappingSelector.tsx
@apps/extension/entrypoints/sidepanel/components/RowIndicator.tsx
@apps/extension/entrypoints/sidepanel/components/capture/StepList.tsx
@apps/extension/src/api/mappings.ts
@apps/extension/src/types/messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure Preencher tab layout with compact header, sticky fill button, and empty state</name>
  <files>
    apps/extension/entrypoints/sidepanel/App.tsx
    apps/extension/entrypoints/sidepanel/components/ConnectedIndicator.tsx
    apps/extension/entrypoints/sidepanel/components/FillControls.tsx
  </files>
  <action>
    **ConnectedIndicator.tsx** - Convert from block element to compact inline badge:
    - Replace the current green box with a small colored dot (8px circle) + text label
    - Green dot with "Connected" text when authenticated
    - Position it in the header row next to the Populatte logo (replaces the current `mb-4` block placement)
    - Make it a compact `flex items-center gap-1.5` with `text-xs`

    **App.tsx** - Restructure the Preencher tab content:
    1. Move ConnectedIndicator into the header bar (next to "Populatte" h1, before the refresh button):
       - `<ConnectedIndicator />` renders inline in `<header>`, not as a separate block below
    2. Compact selectors grouping: Keep ProjectSelector, BatchSelector, MappingSelector in a `space-y-2` (tighter than current `space-y-3`)
    3. RowIndicator + Fill controls on same visual section: group `RowIndicator` and `FillControls` into a compact bottom area
    4. Make the Fill button area sticky at the bottom:
       - Wrap the bottom section (RowIndicator + FillControls) in a `sticky bottom-0 bg-white pt-2 border-t` container
       - The scrollable area above contains selectors, mapping info, and steps list
    5. Add empty state when no batch or mapping is selected:
       - When `!state.batchId`: Show "Selecione um projeto e batch para comecar" with a subtle icon (e.g., Coffee or FileText) centered in the content area, text-gray-400
       - When `state.batchId && !state.hasMapping && !captureMode`: show the existing "Criar Mapping" button (already there)
    6. Remove the outer `space-y-4` on Preencher content; use flex layout with scrollable middle and sticky bottom instead
    7. Add state for mapping steps data: `const [mappingSteps, setMappingSteps] = useState<MappingStep[]>([])` (import MappingStep from api/mappings)
    8. Add state for step validation results: `const [stepValidation, setStepValidation] = useState<Map<string, boolean>>(new Map())`
    9. When `state.mappingId` changes (or on initial load after mapping detected), fetch the mapping with steps:
       ```
       useEffect(() => {
         if (state?.projectId && state?.mappingId) {
           fetchMappingWithSteps(state.projectId, state.mappingId)
             .then(mapping => setMappingSteps(mapping.steps))
             .catch(err => console.error('[App] Failed to fetch mapping steps:', err));
         } else {
           setMappingSteps([]);
         }
       }, [state?.projectId, state?.mappingId]);
       ```
    10. Pass `mappingSteps` and `stepValidation` down to PreencherStepList (created in Task 2)

    **FillControls.tsx** - Adapt for compact sticky layout:
    - Remove outer `space-y-3` wrapper, use tighter spacing
    - Keep existing functionality intact (Fill, Next, Mark Error buttons)
  </action>
  <verify>
    - Extension builds without errors: `cd apps/extension && npm run build`
    - ConnectedIndicator renders as compact dot in header
    - Empty state message appears when no batch selected
    - Fill controls are positioned at bottom of the panel
  </verify>
  <done>
    - Connection status is a compact colored dot in the header area
    - Fill controls area is sticky at bottom of panel
    - Empty state shows guided prompt "Selecione um projeto e batch para comecar" when no batch selected
    - Mapping steps are fetched when a mapping is selected and stored in state
    - Layout fits within ~320px Side Panel width
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PreencherStepList component with collapsible view, action icons, validation badges, and drag-and-drop</name>
  <files>
    apps/extension/entrypoints/sidepanel/components/preencher/PreencherStepList.tsx
    apps/extension/entrypoints/sidepanel/components/preencher/index.ts
    apps/extension/entrypoints/sidepanel/components/index.ts
  </files>
  <action>
    **Create `apps/extension/entrypoints/sidepanel/components/preencher/` directory** with:

    **PreencherStepList.tsx** - Read-only (display) steps list for the Preencher tab:

    Props interface:
    ```typescript
    interface PreencherStepListProps {
      steps: MappingStep[];         // From fetchMappingWithSteps
      validation: Map<string, boolean>;  // stepId -> isValid (true = found on page)
      fillResults: Map<string, 'success' | 'failed'>;  // Per-step fill status
      onStepClick: (step: MappingStep) => void;  // Highlight element on page
      onReorder: (steps: MappingStep[]) => void; // Drag-and-drop reorder
    }
    ```

    Implementation:
    1. **Collapsible container** - Collapsed by default (`const [expanded, setExpanded] = useState(false)`)
    2. **Header row**: "Passos ({count})" with ChevronDown/ChevronUp toggle icon
       - If validation has invalid steps, show summary: "X de Y passos com problema" in `text-xs text-red-500` next to the count
    3. **When expanded**, show the step list using dnd-kit (same pattern as capture/StepList.tsx):
       - Use `@dnd-kit/core` and `@dnd-kit/sortable` (already in dependencies from capture StepList)
       - Each step row shows:
         - Drag handle (GripVertical icon)
         - Action icon: Pencil for 'fill', MousePointer for 'click', Clock for 'wait' (same as capture StepList)
         - Step number badge (blue circle)
         - Source field key or fixed value or "(click only)" or "{waitMs}ms" - truncated with ellipsis
         - Selector text in `text-xs text-gray-500` - truncated
         - Red dot badge if `validation.get(stepId) === false`:
           - Small red circle (w-2 h-2 bg-red-500 rounded-full)
           - Hover tooltip: "Seletor nao encontrado: {selector.value}" using CSS group-hover pattern (same as TabBar tooltip)
         - Fill result indicator (if fillResults has entry):
           - Green check (CheckCircle) for success
           - Red cross (XCircle) for failed
    4. **Step row click**: Call `onStepClick(step)` (highlight on page)
    5. **Drag-and-drop**: Same pattern as capture/StepList.tsx using DndContext, SortableContext, useSortable
       - On drag end, call `onReorder` with reordered array (stepOrder updated)
    6. **Empty list state**: If steps is empty array, show "Nenhum passo configurado" text
    7. Keep the entire panel scrollable (no internal scroll container per locked decision - "No max height, entire panel scrolls")

    **index.ts** - Barrel export:
    ```typescript
    export { PreencherStepList } from './PreencherStepList';
    ```

    **Update components/index.ts** - Add export:
    ```typescript
    export * from './preencher';
    ```

    **Wire in App.tsx**: In the Preencher tab content, after the MappingSelector section and before the sticky footer:
    ```tsx
    {mappingSteps.length > 0 && (
      <PreencherStepList
        steps={mappingSteps}
        validation={stepValidation}
        fillResults={fillResultsMap}
        onStepClick={handleStepHighlight}
        onReorder={handleStepReorder}
      />
    )}
    ```
    - `handleStepHighlight` will be a stub for now (console.log), wired in Plan 02
    - `handleStepReorder` updates local `mappingSteps` state with new order
    - `fillResultsMap` is derived from the fill execution result when FILL_PROGRESS comes back
  </action>
  <verify>
    - Extension builds without errors: `cd apps/extension && npm run build`
    - PreencherStepList renders as collapsed section with "Passos (N)" header
    - Clicking chevron expands to show all steps
    - Steps display action icon, step number, source field, selector text
    - Drag-and-drop handles render and DndContext is functional
  </verify>
  <done>
    - Steps list appears below mapping info, collapsed by default
    - Chevron toggle expands/collapses the list
    - Each step shows action icon + step number + selector + source field
    - Red dot badges appear for steps where validation says invalid
    - Summary "X de Y passos com problema" shows in header when there are invalid steps
    - Per-step fill result indicators (check/cross) appear after fill execution
    - Steps are reorderable via drag-and-drop
    - All content fits within ~320px without horizontal scroll
  </done>
</task>

</tasks>

<verification>
1. `cd apps/extension && npm run build` completes without errors
2. Side Panel opens and shows Preencher tab with compact layout
3. Connection status is a compact dot in the header (not a block element)
4. When no batch selected, empty state prompt is visible
5. When mapping selected, steps list header "Passos (N)" appears (collapsed)
6. Expanding steps list shows all step rows with icons and selectors
7. Fill button is sticky at bottom, always visible even when scrolling
8. Layout fits within ~320px panel width without horizontal scroll
</verification>

<success_criteria>
- FILL-01: Connection status, selectors, mapping info, row nav, fill controls all work in Preencher tab
- FILL-02: Steps list below mapping name with count, action icons, selector, source field
- FILL-04: Warning badge (red dot) on steps where selector not found
- FILL-05: Layout fits ~320px Side Panel width
- Steps list collapsed by default per locked decision
- Drag-and-drop reordering works per locked decision
- Per-step fill result indicators (check/cross) per locked decision
</success_criteria>

<output>
After completion, create `.planning/phases/37-aba-preencher/37-01-SUMMARY.md`
</output>
