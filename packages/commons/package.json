{
  "name": "@populatte/commons",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utilities and Zod schemas for Populatte",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./utils": "./src/utils/index.ts",
    "./schemas": "./src/schemas/index.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\" --fix",
    "type-check": "tsc --noEmit",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@populatte/types": "*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@populatte/eslint-config": "*",
    "@populatte/tsconfig": "*",
    "@types/jest": "^29.5.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.3"
  }
}
