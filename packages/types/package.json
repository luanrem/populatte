{
  "name": "@populatte/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types and interfaces for Populatte",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./user": "./src/user.types.ts",
    "./api": "./src/api.types.ts",
    "./common": "./src/common.types.ts"
  },
  "scripts": {
    "lint": "eslint \"src/**/*.ts\" --fix",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@populatte/eslint-config": "*",
    "@populatte/tsconfig": "*",
    "typescript": "^5.3.3"
  }
}
